import { expect } from "chai";

export function shouldBehaveLikeHypercertMinterUpgrade(): void {
  it.skip("supports upgrader role", async function () {
    expect(0);
  });

  it.skip("checks on faulty upgrades", async function () {
    expect(0);
  });
}
